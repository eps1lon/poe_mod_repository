(function (__undefined) {
    'use strict';
    // just some interchangeable requires to see if it works with node
    var src_path = require("./src_path");
    
    var node_module = require("../../js/gui");
}).call(this);