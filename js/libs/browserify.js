(function () {
    /**
     * empty require function so that we dont have to run browserify in dev
     * @param {type} path
     * @returns {undefined}
     */
    this.require = function (path) {  };
})();